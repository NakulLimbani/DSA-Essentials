/*#include <stdio.h>
int main(){
    int Arr[20];
    int found=0 , pos=-1 ,s, num;
    printf("Enter Number of elements");
    scanf("%d",&num);
    printf("Entetr Elembents");
    for(int i=0; i<num ; i++)
    {
        scanf("%d",&Arr[i]);
    }
    printf("Enter Element to be searched");
    scanf("%d",&s);
    for(int i=0; i<num ;i++)
    {
        if(Arr[i]==s)
        {
            found = 1;
            pos =i;
            printf("%d Element is found at %d pos",s,i+1);
            break;
        }
        
    }
    if(found == 0)
    {
        printf("Not Exist");
    }
    return 0;
}
*/
